import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.companion.sahaapp',
  appName: 'saha-your-safe-companion-main',
  webDir: 'dist'
};

export default config;
